"""JobFinder — a local job-search assistant."""

# Kept equal to the version in `pyproject.toml`, which names the built exe.
# `/healthz` reports this one, and `test_the_package_version_matches_pyproject`
# is what stops the two drifting apart again.
__version__ = "0.7.0"
